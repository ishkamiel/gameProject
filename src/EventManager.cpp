#include "EventManager.h"

namespace pdGame 
{
    static EventManager em;

    EventManager& GetEventManager();

    EventManager::EventManager()
    {
    }

    void EventManager::initialize()
    {
    }

    bool EventManager::AddListener(std
}
